<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"
    >

    <!-- FOndo claro dentro del campo -->
    <solid android:color="@color/gris_fondo_input"/>
    <!-- Curvatura suave -->
    <solid android:radius="12dp"/>
    <!-- Linea de contorno -->
    <solid android:width="1.5dp" android:color="@color/gris_borde_input"/>
</shape>
