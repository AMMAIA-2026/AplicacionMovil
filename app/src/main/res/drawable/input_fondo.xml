<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Redondeo suave de las esquinas -->
    <corners android:radius="8dp" />

    <!-- Color de relleno claro para el interior del campo -->
    <solid android:color="@color/input_fondo" />

    <!-- Línea de borde fino alrededor de la caja -->
    <stroke
        android:width="1dp"
        android:color="@color/input_borde" />

</shape>